steps:
- block: Request release
  fields:
  - default: ALL
    key: region
    options:
    - label: us-east-2
      value: us-east-2
    - label: ALL
      value: ALL
    prompt: Please select a region for deployment
    select: Region
- command: buildkite-agent pipeline upload .buildkite/workflows/deploy-production-$(buildkite-agent
    meta-data get region).yml
  label: Deploy production environment
